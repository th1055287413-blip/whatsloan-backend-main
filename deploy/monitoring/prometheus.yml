global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "whatsmeow-api"
    static_configs:
      - targets: ["localhost:8080"]

  - job_name: "whatsmeow-connector"
    static_configs:
      - targets: ["localhost:8785"]
